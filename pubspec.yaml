name: whatssting
description: WhatsTing - Aplikasi Pesan & Sosial Media Serba Bisa
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^1.1.0

  # Reels / Video
  video_player: ^2.8.6
  chewie: ^1.8.5


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/videos/
    - assets/images/